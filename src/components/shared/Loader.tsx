import { Loader2 as LoaderIcon } from 'lucide-react';

const Loader = () => (
  <div className="flex justify-center items-center animate-spin m-0 p-0 w-10 h-10">
    <LoaderIcon />
  </div>
);

export default Loader;
